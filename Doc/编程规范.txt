一、命名规范
    1. 类型名
        首字母大写，不加任何前缀；
        接口类型使用I开头；

    2. 成员变量
        class的成员变量使用m_前缀，若是指针，则m_后接p类型前缀，其他类型均不加类型前缀；
        struct的成员变量不使用m_前缀；
        若没有合适的类型前缀，则直接首字母大写即可；

    3. 全局变量
        全局变量使用g_前缀；
